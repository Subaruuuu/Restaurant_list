<div class="container search-bar">
  <div class="search-bar-outer">
    <div class="search-bar-inner">
      <form action="/search">
        <h4 class="mb-2">尋找餐廳：</h4>
        <div class="input-group">
          <input type="text" name="keyword" class="form-control" placeholder="輸入餐廳、分類" aria-label="Restaurant Name..."
            aria-describedby="search-button" value={{keyword}}>
          <div class=" input-group-append">
            <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
          </div>
        </div>
      </form>
    </div>
  </div>
</div>

<div class="container mt-5 col-9">
  <div class="row d-flex justify-content-around">
    <div>
      <a class="btn btn-outline-primary" href="/restaurants/new" role="button">新增餐廳</a>
    </div>
    {{!-- <form action="/search">
      <div class="input-group">
        <input type="text" name="keyword" class="" placeholder="輸入餐廳、分類" aria-label="Restaurant Name..."
          aria-describedby="search-button" value={{keyword}}>
        <div class=" input-group-append">
          <button class="btn btn-major" type="submit" id="search-button"><i class="fas fa-search"></i></button>
        </div>
      </div>
    </form> --}}
    <form action="/">
      <div class="dropdown">
        <a class="btn btn-secondary dropdown-toggle" href="#" role="button" id="dropdownMenuLink" data-toggle="dropdown"
          aria-haspopup="true" aria-expanded="false">
          排序方法
        </a>

        <div class="dropdown-menu" aria-labelledby="dropdownMenuLink">
          <a class="dropdown-item" href="/sort?option=asc">A -> Z</a>
          <a class="dropdown-item" href="/sort?option=desc">Z -> A</a>
          <a class="dropdown-item" href="/sort?option=category">類別</a>
          <a class="dropdown-item" href="/sort?option=location">地區</a>
        </div>
      </div>
    </form>

  </div>


</div>





<div class="container mt-5">
  <div class="row">
    <div class="col-md-10 col-12 mx-auto">
      <div class="card-columns">

        {{#each restaurant}}
        {{!-- <a href="/restaurants/{{this.id}}" class="text-secondary"> --}}
          <div class="card mb-3">
            <img class="card-img-top" src={{this.image}} alt={{this.name}}>
            <div class="card-body p-3">
              <h6 class="card-title mb-1">{{this.name}}</h6>

              <div class="restaurant-category mb-1">
                <i class="fas fa-utensils pr-2"></i> {{this.category}}
              </div>

              <span class="badge badge-pill badge-danger font-weight-normal">
                {{this.rating}}
                <i class="fas fa-star fa-xs"></i>
              </span>

              <div class="d-flex justify-content-start mt-2">
                <a href="/restaurants/{{this._id}}/edit"><i class="fas fa-edit mr-2"></i></a>
                <a href="/restaurants/{{this._id}}"><i class="fas fa-eye mr-2"></i></a>

                {{!-- 另外一種刪除寫法 --}}
                <form action="/restaurants/{{this._id}}?_method=DELETE" method="post">
                  <a href="#" onclick="this.parentNode.submit()"><i class="fas fa-trash-alt mr-2"></i></a>
                </form>

              </div>

            </div>
          </div>
          {{!--
        </a> --}}
        {{/each}}

      </div>
    </div>
  </div>
</div>